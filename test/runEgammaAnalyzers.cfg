#
#  Author: Michael A. Balazs, University of Virginia
#  $Id: egammaAnalysis.cfg,v 1.0 2006/11/13 15:23:02 mabalazs Exp $
#
process egammaAnalysis =
{

  source = PoolSource {
    # Number of events program should run
    untracked vstring fileNames = {'file:egamma_electrons.root'}
    untracked int32 maxEvents = -1
  }

  # DQM services
  service = DaqMonitorROOTBackEnd{ }

	block VerificationCommonParameters =
	{
	  untracked string outputFile = "egammaVerificationAnalyzer.root"
	  untracked string CMSSW_Version = "V1_2_0_pre4"

	  untracked bool verboseDBE = true
	}

  # analyzer to make histos from basic clusters
  include "Validation/RecoEgamma/data/egammaElectronAnalyzer.cfi"

  # analyzer to make histos from super clusters
  #include "Validation/RecoEgamma/data/egammaSCAnalyzer.cfi"

  #path p = { egammaBasicClusterAnalyzer & egammaSuperClusterAnalyzer }
	 path p = { egammaElectronAnalyzer}

}

